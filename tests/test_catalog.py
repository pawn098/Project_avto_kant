"""Проверка кликабельности разделов во вкладке каталог"""
from pages.catalog_page import catalog_page
from pages.main_page import main_page


def test_01(set_up):
    driver1 = set_up
    mp = main_page(driver1)
    mp.open_catalog_page()

    cp = catalog_page(driver1)
    cp.open_Cloth()
def test_02(set_up):
    driver1 = set_up
    mp = main_page(driver1)
    mp.open_catalog_page()

    cp = catalog_page(driver1)
    cp.open_Shoes()
def test_03(set_up):
    driver1 = set_up
    mp = main_page(driver1)
    mp.open_catalog_page()

    cp = catalog_page(driver1)
    cp.open_Snowboarding_equipment()
def test_04(set_up):
    driver1 = set_up
    mp = main_page(driver1)
    mp.open_catalog_page()

    cp = catalog_page(driver1)
    cp.open_Alpine_skiing()
def test_05(set_up):
    driver1 = set_up
    mp = main_page(driver1)
    mp.open_catalog_page()

    cp = catalog_page(driver1)
    cp.open_Cross_country_skiing()
def test_06(set_up):
    driver1 = set_up
    mp = main_page(driver1)
    mp.open_catalog_page()

    cp = catalog_page(driver1)
    cp.open_run()
def test_07(set_up):
    driver1 = set_up
    mp = main_page(driver1)
    mp.open_catalog_page()

    cp = catalog_page(driver1)
    cp.open_Tourism()
def test_08(set_up):
    driver1 = set_up
    mp = main_page(driver1)
    mp.open_catalog_page()

    cp = catalog_page(driver1)
    cp.open_Optics_helmets_protection()
def test_09(set_up):
    driver1 = set_up
    mp = main_page(driver1)
    mp.open_catalog_page()

    cp = catalog_page(driver1)
    cp.open_Backpacks_bags_covers()
def test_10(set_up):
    driver1 = set_up
    mp = main_page(driver1)
    mp.open_catalog_page()

    cp = catalog_page(driver1)
    cp.open_Bicycles()
def test_11(set_up):
    driver1 = set_up
    mp = main_page(driver1)
    mp.open_catalog_page()

    cp = catalog_page(driver1)
    cp.open_Mountaineering()
def test_12(set_up):
    driver1 = set_up
    mp = main_page(driver1)
    mp.open_catalog_page()

    cp = catalog_page(driver1)
    cp.open_Accessories()
def test_13(set_up):
    driver1 = set_up
    mp = main_page(driver1)
    mp.open_catalog_page()

    cp = catalog_page(driver1)
    cp.open_Scooters()
def test_14(set_up):
    driver1 = set_up
    mp = main_page(driver1)
    mp.open_catalog_page()

    cp = catalog_page(driver1)
    cp.open_Rollers()
def test_15(set_up):
    driver1 = set_up
    mp = main_page(driver1)
    mp.open_catalog_page()

    cp = catalog_page(driver1)
    cp.open_Skateboards_longboards()
def test_16(set_up):
    driver1 = set_up
    mp = main_page(driver1)
    mp.open_catalog_page()

    cp = catalog_page(driver1)
    cp.open_Roller_skis()
def test_17(set_up):
    driver1 = set_up
    mp = main_page(driver1)
    mp.open_catalog_page()

    cp = catalog_page(driver1)
    cp.open_Swimming()
def test_18(set_up):
    driver1 = set_up
    mp = main_page(driver1)
    mp.open_catalog_page()

    cp = catalog_page(driver1)
    cp.open_SUP_Boards_Kayaks()